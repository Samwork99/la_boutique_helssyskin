{% extends 'base.html.twig' %}

{% block title %}Confirmation de commande!{% endblock %}

{% block content %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <br>
            <h1 class="text-center">Confirmation de votre commande</h1><br>
            <p>Bonjour <strong>{{ order.user.firstname }} {{ order.user.lastname }}</strong>,<br><br>
                Le paiement a été accepté. Nous vous remercions pour votre commande n°<strong>{{ order.reference }}</strong>.
            <br>
            Une confirmation vient de vous être envoyée par email à l'adresse suivante : <strong>{{ order.user.email }}</strong>.<hr>

            Votre commande sera livrée par <strong>{{ order.carrierName }}</strong> à l'adresse suivante : <br>
            {{ order.delivery|raw }}
            <hr>
            Pour suivre votre commande, rendez-vous dans votre <a href="{{ path('account_order') }}" style="color:blue">espace client HelssySkin.</a></p>
            <p 
                class="text-center-left"><a href="{{ path('cart') }}" style="color:blue">Retourner au panier</a>
                | <a href="{{ path('home') }}" style="color:blue">Retourner à l'accueil</a>
            </p><br>
            
        </div>
    </div>
</div>

{% endblock %}
