{% extends 'base.html.twig' %}

{% block title %}Gérer mes adresses{% endblock %}

{% block content %}
<body class="first_address">
    <div class="container">
        <br>
        <h2 class="title_address">Ajouter une adresse</h2>
            <a href="{{ path('account_address') }}">Retour</a>
            <hr>
        {{ form_start(form) }}
            <div class="row justify-content-center">
                <div class="col-md-8 col-xl-6 my-3 mask-custom rounded-5 shadow-5-strong py-3 mb-5" id="page_address">
                    <div class="row">
                        <div class="col-md-12">{{ form_row(form.name) }}</div>
                        <div class="col-md-6">{{ form_row(form.lastname) }}</div>
                        <div class="col-md-6">{{ form_row(form.firstname) }}</div>
                        <div class="col-md-12">{{ form_row(form.compagny) }}</div>
                        <div class="col-md-6">{{ form_row(form.address) }}</div>
                        <div class="col-md-6">{{ form_row(form.postal) }}</div>
                        <div class="col-md-6">{{ form_row(form.city) }}</div>
                        <div class="col-md-12">{{ form_row(form.country) }}</div>
                        <div class="col-md-6">{{ form_row(form.phone) }}</div>
                    </div>
        {{ form_end(form) }}
                </div>
            </div>
        
    </div>
</body>
{% endblock %}